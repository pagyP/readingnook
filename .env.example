# Flask configuration
FLASK_ENV=development
FLASK_DEBUG=True

# Security - Generate a strong secret key for production
# You can generate one with: python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=your-secret-key-here-generate-a-strong-one-for-production
# Database configuration (used by docker-compose.yml to construct SQLALCHEMY_DATABASE_URI)
# Default values are set in docker-compose.yml, but you can override them here
DB_USER=readingnook
DB_PASSWORD=changeme
DB_NAME=readingnook

# Security for production deployments
SESSION_COOKIE_SECURE=False